# Finance Tracker — App locale (double-clic Windows) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformer finance-tracker (2 conteneurs Docker sur NAS) en une app locale sur le PC dev (WSL) démarrée/arrêtée par un double-clic sur une icône Windows, servie par un seul process Node.

**Architecture:** Le backend NestJS sert l'API (`/api/*`) ET le frontend React buildé (tout le reste) sur un seul port via `@nestjs/serve-static`. Un script WSL lance/arrête le process ; des wrappers VBS Windows + raccourcis Bureau pilotent le tout. Les données sont rapatriées du NAS une fois.

**Tech Stack:** NestJS 11 (Express 5), `@nestjs/serve-static` 5, React + Vite, Node v22 (WSL), VBScript + PowerShell (Windows), rsync/ssh (NAS port SSH custom).

## Global Constraints

- Un seul process Node, un seul port : `PORT=3000` (depuis `backend/.env`).
- API sous préfixe `/api` (déjà `app.setGlobalPrefix('api')`) ; le frontend buildé est servi pour toute autre route (fallback SPA).
- Le frontend utilise déjà des URLs relatives `/api/...` → **aucune modification frontend**.
- `cwd` du process Node = `backend/` (charge `backend/.env`, comme dans le conteneur NAS).
- Chemins de données = **absolus, calculés dans `run.sh`** et exportés avant node (dotenv n'écrase pas une var shell déjà définie) → données à `<repo>/data/`, pas de chemin `$HOME` codé en dur.
- Le backend refuse de booter en `production` sans `APP_PIN` (sécurité fail-safe existante) → garder un `APP_PIN` valide dans `.env`.
- Chemin repo WSL : `/home/sylvain_ladoire/projects/developpeur/finance-tracker`.
- NAS : hôte `nas`, SSH port **622**. Données : `/volume2/docker/developpeur/data/finance/` (+ `.../data/shared/`). Env NAS : `/volume2/docker/developpeur/finance-tracker-v2/backend/.env`.
- Pas de commit auto : committer seulement aux étapes "Commit" du plan.

---

### Task 1: Servir le frontend depuis le backend (ServeStaticModule)

**Files:**
- Modify: `backend/src/app.module.ts`

**Interfaces:**
- Consumes: rien (point de départ).
- Produces: un serveur unique sur le port 3000 où `GET /api/health` → JSON `{status:'ok'}` et `GET /` → `index.html` du frontend buildé.

- [ ] **Step 1: Build complet pour disposer de `frontend/dist` (test manuel de référence)**

Le frontend doit être buildé pour que serve-static ait quelque chose à servir.

Run:
```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker/frontend && npm run build
```
Expected: build Vite réussi, dossier `frontend/dist/index.html` créé.

Vérifier :
```bash
ls /home/sylvain_ladoire/projects/developpeur/finance-tracker/frontend/dist/index.html
```
Expected: le chemin s'affiche (fichier présent).

- [ ] **Step 2: Câbler ServeStaticModule dans app.module.ts**

Ajouter l'import en haut de `backend/src/app.module.ts` (après les imports existants `@nestjs/...`) :

```typescript
import { ServeStaticModule } from '@nestjs/serve-static';
import { join } from 'path';
```

Puis ajouter en **premier élément** du tableau `imports: [` (avant `ConfigModule.forRoot(...)`) :

```typescript
    ServeStaticModule.forRoot({
      // __dirname à l'exécution = backend/dist → remonte vers frontend/dist
      rootPath: join(__dirname, '..', '..', 'frontend', 'dist'),
      // Ne pas intercepter les routes API (Express 5 / path-to-regexp v8)
      exclude: ['/api/{*path}'],
    }),
```

- [ ] **Step 3: Rebuild backend**

Run:
```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker/backend && npm run build
```
Expected: `nest build` réussi, pas d'erreur TypeScript, `backend/dist/main.js` présent.

- [ ] **Step 4: Démarrer le serveur en test (sans .env complet → mode dev)**

Pour ce test isolé, démarrer en `development` (évite l'exigence APP_PIN) avec un répertoire data jetable :

Run (laisser tourner dans un terminal en arrière-plan) :
```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker/backend && \
  NODE_ENV=development PORT=3000 DATA_DIR=/tmp/ft-test-data UPLOAD_DIR=/tmp/ft-test-data/uploads \
  node dist/main &
sleep 4
```
Expected: log `Backend running on http://localhost:3000/api`.

- [ ] **Step 5: Vérifier API + frontend servis sur le même port**

Run:
```bash
echo "--- API ---"; curl -s -o /dev/null -w "%{http_code} %{content_type}\n" http://localhost:3000/api/health
curl -s http://localhost:3000/api/health
echo; echo "--- FRONT ---"; curl -s -o /dev/null -w "%{http_code} %{content_type}\n" http://localhost:3000/
curl -s http://localhost:3000/ | grep -o "<title>[^<]*</title>" | head -1
```
Expected:
- API : `200 application/json` puis `{"status":"ok",...}`
- FRONT : `200 text/html` puis une balise `<title>...</title>` (l'index du frontend, **pas** une 404)

- [ ] **Step 6: Arrêter le serveur de test et nettoyer**

Run:
```bash
pkill -f "node dist/main"; rm -rf /tmp/ft-test-data
```
Expected: process arrêté.

- [ ] **Step 7: Commit**

```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker
git add backend/src/app.module.ts
git commit -m "feat(backend): servir le frontend buildé via ServeStaticModule (1 process, 1 port)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Corriger le script de build racine (Angular → Vite)

**Files:**
- Modify: `package.json` (racine)

**Interfaces:**
- Consumes: les scripts `build` des sous-projets (`backend/package.json` → `nest build`, `frontend/package.json` → `tsc -b && vite build`).
- Produces: `npm run build` à la racine builde backend + frontend correctement.

- [ ] **Step 1: Constater le bug**

Run:
```bash
grep '"build:frontend"' /home/sylvain_ladoire/projects/developpeur/finance-tracker/package.json
```
Expected: affiche `"build:frontend": "cd frontend && npx ng build"` — commande **Angular obsolète** (le frontend est React+Vite).

- [ ] **Step 2: Corriger les scripts build**

Dans `package.json` (racine), remplacer les deux lignes :

```json
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npx ng build"
```
par :
```json
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build"
```

(Seul `build:frontend` change : `npx ng build` → `npm run build`.)

- [ ] **Step 3: Vérifier le build racine de bout en bout**

Run:
```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker && npm run build
```
Expected: backend (`nest build`) puis frontend (`vite build`) réussissent, pas d'erreur `ng: command not found`.

- [ ] **Step 4: Commit**

```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker
git add package.json
git commit -m "fix(build): build:frontend racine utilise Vite (npm run build) au lieu de ng build obsolète

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Rapatrier données + .env depuis le NAS

**Files:**
- Create: `local/sync-from-nas.sh`

**Interfaces:**
- Consumes: accès SSH NAS (port SSH custom).
- Produces: `data/` peuplé (relevés, budgets, uploads, `data/shared/claude-shared.json`) + `backend/.env` local créé depuis le NAS, ajusté pour le local.

- [ ] **Step 1: Écrire le script de rapatriement**

Create `local/sync-from-nas.sh` :

```bash
#!/usr/bin/env bash
# Rapatriement one-shot des données finance-tracker du NAS vers le local (PC dev).
# Idempotent : relançable sans casse (rsync). NE TOUCHE PAS aux données locales plus récentes.
set -euo pipefail

REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
NAS_HOST="nas"
NAS_PORT="22"
NAS_DATA="/volume2/docker/developpeur/data/finance/"
NAS_SHARED="/volume2/docker/developpeur/data/shared/claude-shared.json"
NAS_ENV="/volume2/docker/developpeur/finance-tracker-v2/backend/.env"

echo "==> Rapatriement données finance depuis le NAS ($NAS_HOST:$NAS_PORT)"
mkdir -p "$REPO/data/shared" "$REPO/data/uploads"

# Données applicatives (relevés, budgets, snapshots, uploads...)
rsync -az --info=stats1 -e "ssh -p $NAS_PORT" "$NAS_HOST:$NAS_DATA" "$REPO/data/"

# Solde Claude partagé → copie standalone locale
rsync -az -e "ssh -p $NAS_PORT" "$NAS_HOST:$NAS_SHARED" "$REPO/data/shared/" || \
  echo "   (claude-shared.json absent sur le NAS, ignoré)"

# .env : récupérer la vraie ANTHROPIC_API_KEY + APP_PIN (sans écraser un .env local existant)
if [ -f "$REPO/backend/.env" ]; then
  echo "==> backend/.env existe déjà localement, conservé tel quel"
else
  echo "==> Récupération de backend/.env depuis le NAS"
  scp -P "$NAS_PORT" "$NAS_HOST:$NAS_ENV" "$REPO/backend/.env"
  # Ajustements local : pas de CORS forcé / pas d'hôtes démo
  {
    echo ""
    echo "# --- Overrides locaux (PC dev) ajoutés par sync-from-nas.sh ---"
    echo "NODE_ENV=production"
    echo "PORT=3000"
    echo "CORS_ORIGIN=http://localhost:3000"
  } >> "$REPO/backend/.env"
fi

echo "==> Terminé."
echo "    Données : $REPO/data"
echo "    Env     : $REPO/backend/.env"
```

- [ ] **Step 2: Rendre exécutable et lancer**

Run:
```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker
chmod +x local/sync-from-nas.sh
./local/sync-from-nas.sh
```
Expected: stats rsync affichées, message `Terminé.` sans erreur.

- [ ] **Step 3: Vérifier les données et l'env rapatriés**

Run:
```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker
ls data/ && echo "--- shared ---" && ls data/shared/
echo "--- env keys (valeurs masquées) ---"
grep -E "^(APP_PIN|ANTHROPIC_API_KEY|PORT|NODE_ENV)=" backend/.env | sed 's/=.*/=***/'
```
Expected:
- `data/` contient `budgets.json`, `loans.json`, etc. + `data/shared/claude-shared.json`.
- `backend/.env` contient bien les clés `APP_PIN`, `ANTHROPIC_API_KEY`, `PORT`, `NODE_ENV`.

- [ ] **Step 4: Commit (le script seulement — `data/` et `.env` sont gitignorés)**

```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker
git add local/sync-from-nas.sh
git commit -m "chore(local): script one-shot de rapatriement données + .env depuis le NAS

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Scripts de lancement/arrêt WSL

**Files:**
- Create: `local/run.sh`
- Create: `local/stop.sh`

**Interfaces:**
- Consumes: `backend/dist/main.js` + `frontend/dist/` (Task 1), `backend/.env` + `data/` (Task 3).
- Produces:
  - `local/run.sh` : build-si-besoin, exporte les chemins data absolus, lance `node dist/main` (cwd=backend), écrit le PID dans `data/.pid`, logs dans `logs/finance.log`. No-op si déjà démarré.
  - `local/stop.sh` : arrête le process via `data/.pid` (fallback `pkill -f`).

- [ ] **Step 1: Écrire `local/run.sh`**

Create `local/run.sh` :

```bash
#!/usr/bin/env bash
# Démarre finance-tracker en local (PC dev). Idempotent : no-op si déjà up.
set -euo pipefail

REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PORT="3000"
PIDFILE="$REPO/data/.pid"
LOGDIR="$REPO/logs"
mkdir -p "$LOGDIR" "$REPO/data"

# Déjà démarré ? (santé OK) → ne rien faire
if curl -sf "http://localhost:$PORT/api/health" >/dev/null 2>&1; then
  echo "Déjà démarré (port $PORT répond)."
  exit 0
fi

# Build si artefacts manquants
if [ ! -f "$REPO/backend/dist/main.js" ] || [ ! -f "$REPO/frontend/dist/index.html" ]; then
  echo "Build manquant → npm run build…"
  (cd "$REPO" && npm run build)
fi

# Chemins de données ABSOLUS (priment sur backend/.env car dotenv n'écrase pas process.env)
export DATA_DIR="$REPO/data"
export UPLOAD_DIR="$REPO/data/uploads"
export SHARED_DATA_DIR="$REPO/data/shared"

cd "$REPO/backend"
echo "Démarrage node dist/main (port $PORT)…"
# setsid → nouvelle session : le process survit à la fermeture du wsl.exe lanceur
# (un simple nohup depuis un shell qui se ferme reste fragile, cf. mémoire launcher WSL).
setsid node dist/main >>"$LOGDIR/finance.log" 2>&1 < /dev/null &
echo $! > "$PIDFILE"

# Attendre que le serveur réponde (max ~20s)
for i in $(seq 1 20); do
  if curl -sf "http://localhost:$PORT/api/health" >/dev/null 2>&1; then
    echo "Prêt sur http://localhost:$PORT (pid $(cat "$PIDFILE"))"
    exit 0
  fi
  sleep 1
done

echo "ERREUR : le serveur n'a pas répondu après 20s. Voir $LOGDIR/finance.log" >&2
exit 1
```

- [ ] **Step 2: Écrire `local/stop.sh`**

Create `local/stop.sh` :

```bash
#!/usr/bin/env bash
# Arrête finance-tracker en local.
set -uo pipefail

REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PIDFILE="$REPO/data/.pid"

if [ -f "$PIDFILE" ]; then
  PID="$(cat "$PIDFILE")"
  if kill "$PID" 2>/dev/null; then
    echo "Arrêté (pid $PID)."
  fi
  rm -f "$PIDFILE"
fi

# Filet de sécurité : tuer tout node dist/main résiduel de ce repo
pkill -f "node dist/main" 2>/dev/null && echo "Process résiduels nettoyés." || true
echo "Stop terminé."
```

- [ ] **Step 3: Rendre exécutables et tester le cycle complet**

Run:
```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker
chmod +x local/run.sh local/stop.sh
./local/run.sh
```
Expected: `Prêt sur http://localhost:3000 (pid ...)`.

- [ ] **Step 4: Vérifier que l'app sert les vraies données rapatriées**

Run:
```bash
echo "--- health ---"; curl -s http://localhost:3000/api/health
echo; echo "--- une route data (budgets) ---"
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/api/budgets
echo "--- front ---"; curl -s -o /dev/null -w "%{http_code} %{content_type}\n" http://localhost:3000/
```
Expected: health `{"status":"ok"...}` ; `/api/budgets` répond `200` (ou `401` si PIN requis — dans les deux cas **pas** 404/500, ce qui prouve que le backend tourne avec les données) ; front `200 text/html`.

- [ ] **Step 5: Tester l'arrêt**

Run:
```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker
./local/stop.sh
sleep 1
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/api/health || echo "down (connexion refusée = OK)"
pgrep -f "node dist/main" && echo "ENCORE VIVANT (KO)" || echo "process bien arrêté"
```
Expected: `Stop terminé.`, puis `process bien arrêté`.

- [ ] **Step 6: Commit**

```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker
git add local/run.sh local/stop.sh
git commit -m "feat(local): scripts WSL run.sh/stop.sh (build-si-besoin, pid, logs, idempotent)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Wrappers Windows (VBS) + installateur de raccourcis Bureau

**Files:**
- Create: `local/start.vbs`
- Create: `local/stop.vbs`
- Create: `local/install-shortcuts.ps1`

**Interfaces:**
- Consumes: `local/run.sh` + `local/stop.sh` (Task 4) côté WSL ; `finance-tracker.ico` (présent à la racine).
- Produces: deux raccourcis Bureau Windows ("Finance Tracker" et "Finance Tracker — Stop") pointant vers les VBS.

> Note WSL : le chemin WSL du projet est `/home/sylvain_ladoire/projects/developpeur/finance-tracker`. Les VBS appellent `wsl.exe bash -lic "..."` (login+interactive shell pour charger node via le profil, cf. mémoire `live_job_status_ui_pattern` / launcher fiable = chaîne `bash -lc` unique).

- [ ] **Step 1: Écrire `local/start.vbs`**

Create `local/start.vbs` :

```vbscript
' Finance Tracker — Démarrage (fenêtre cachée) + ouverture du navigateur.
Option Explicit
Dim ws, proj, url, i, rc
Set ws = CreateObject("WScript.Shell")
proj = "/home/sylvain_ladoire/projects/developpeur/finance-tracker"
url  = "http://localhost:3000"

' 1) Lancer le serveur en tâche de fond, fenêtre cachée (0), sans attendre (False)
ws.Run "wsl.exe bash -lic ""cd " & proj & " && ./local/run.sh""", 0, False

' 2) Attendre que le serveur réponde (max ~30s) avant d'ouvrir le navigateur
For i = 1 To 30
    rc = ws.Run("wsl.exe bash -lic ""curl -sf " & url & "/api/health >/dev/null""", 0, True)
    If rc = 0 Then Exit For
    WScript.Sleep 1000
Next

' 3) Ouvrir le navigateur par défaut
ws.Run url, 1, False
```

- [ ] **Step 2: Écrire `local/stop.vbs`**

Create `local/stop.vbs` :

```vbscript
' Finance Tracker — Arrêt.
Option Explicit
Dim ws, proj
Set ws = CreateObject("WScript.Shell")
proj = "/home/sylvain_ladoire/projects/developpeur/finance-tracker"
ws.Run "wsl.exe bash -lic ""cd " & proj & " && ./local/stop.sh""", 0, True
```

- [ ] **Step 3: Écrire `local/install-shortcuts.ps1`**

Create `local/install-shortcuts.ps1` :

```powershell
# Crée/maj les 2 raccourcis Bureau Finance Tracker (à lancer une fois depuis Windows).
$ErrorActionPreference = "Stop"

# Chemin Windows du repo (UNC \\wsl$). Adapter la distro si besoin (wsl -l -q).
$repoWin = "\\wsl.localhost\Ubuntu\home\sylvain_ladoire\projects\developpeur\finance-tracker"
if (-not (Test-Path $repoWin)) {
    $repoWin = "\\wsl$\Ubuntu\home\sylvain_ladoire\projects\developpeur\finance-tracker"
}
if (-not (Test-Path $repoWin)) {
    throw "Repo introuvable via \\wsl. Vérifie le nom de la distro (wsl -l -q) et adapte le script."
}

$desktop = [Environment]::GetFolderPath("Desktop")
$icon    = Join-Path $repoWin "finance-tracker.ico"
$ws      = New-Object -ComObject WScript.Shell

function New-FtShortcut($name, $vbs, $desc) {
    $lnk = $ws.CreateShortcut((Join-Path $desktop "$name.lnk"))
    $lnk.TargetPath       = "wscript.exe"
    $lnk.Arguments        = '"' + (Join-Path $repoWin ("local\" + $vbs)) + '"'
    $lnk.WorkingDirectory = $repoWin
    $lnk.IconLocation     = $icon
    $lnk.Description       = $desc
    $lnk.Save()
}

New-FtShortcut "Finance Tracker"        "start.vbs" "Démarre Finance Tracker (local) et ouvre le navigateur"
New-FtShortcut "Finance Tracker - Stop" "stop.vbs"  "Arrête Finance Tracker (local)"

Write-Host "Raccourcis créés sur le Bureau."
```

- [ ] **Step 4: Vérifier la syntaxe et le nom de distro (côté WSL)**

Run:
```bash
ls /home/sylvain_ladoire/projects/developpeur/finance-tracker/local/*.vbs
wsl.exe -l -q 2>/dev/null | head || echo "wsl.exe -l non dispo depuis WSL (normal) — vérifier la distro côté Windows"
```
Expected: les 2 `.vbs` listés. Noter le nom exact de la distro pour valider `install-shortcuts.ps1` (par défaut `Ubuntu`).

> **Validation manuelle (utilisateur, côté Windows)** : ces étapes ne peuvent pas être exécutées depuis WSL.
> 1. Ouvrir PowerShell Windows, lancer `& "\\wsl.localhost\Ubuntu\home\sylvain_ladoire\projects\developpeur\finance-tracker\local\install-shortcuts.ps1"` (ou via l'Explorateur : clic droit → Exécuter avec PowerShell).
> 2. Double-cliquer "Finance Tracker" → le navigateur doit s'ouvrir sur le dashboard avec les données.
> 3. Double-cliquer "Finance Tracker — Stop" → vérifier l'arrêt (`pgrep -f "node dist/main"` vide côté WSL).

- [ ] **Step 5: Commit**

```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker
git add local/start.vbs local/stop.vbs local/install-shortcuts.ps1
git commit -m "feat(local): wrappers VBS Windows + installateur de raccourcis Bureau

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: Supprimer les anciens launchers + vérification de bout en bout + doc

**Files:**
- Delete: `start.bat`, `launch.vbs`, `start-bg.ps1` (racine, chemins `C:\Developpeur` morts — gitignorés, jamais committés)
- Modify: `README.md`

**Interfaces:**
- Consumes: tout ce qui précède (Tasks 1-5).
- Produces: repo propre, README documentant le lancement local.

- [ ] **Step 1: Supprimer les anciens launchers obsolètes**

Run:
```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker
rm -f start.bat launch.vbs start-bg.ps1
ls start.bat launch.vbs start-bg.ps1 2>&1 | head
```
Expected: `No such file or directory` pour les trois (supprimés). (Ils sont gitignorés → pas de `git rm` nécessaire.)

- [ ] **Step 2: Documenter le lancement local dans le README**

Dans `README.md`, ajouter une section (après la section d'intro/installation existante) :

```markdown
## Lancement local (WSL)

L'app tourne en local en **un seul process Node** (le backend NestJS sert l'API
ET le frontend buildé sur `http://localhost:3000`).

### Première installation
```bash
./local/sync-from-nas.sh        # rapatrie données + .env depuis le NAS (une fois)
npm run build                   # build backend + frontend
```
Puis, côté Windows (PowerShell) : exécuter `local/install-shortcuts.ps1`
pour créer les raccourcis Bureau.

### Au quotidien
- **Double-clic "Finance Tracker"** → démarre + ouvre le navigateur.
- **Double-clic "Finance Tracker — Stop"** → arrête.

En ligne de commande (WSL) : `./local/run.sh` / `./local/stop.sh`.
```

- [ ] **Step 3: Vérification finale de bout en bout (WSL)**

Run:
```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker
./local/stop.sh >/dev/null 2>&1 || true
npm run build
./local/run.sh
echo "--- health ---"; curl -s http://localhost:3000/api/health
echo; echo "--- front ---"; curl -s -o /dev/null -w "%{http_code} %{content_type}\n" http://localhost:3000/
echo "--- data route ---"; curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/api/budgets
./local/stop.sh
```
Expected: build OK → `Prêt sur http://localhost:3000` → health `ok` → front `200 text/html` → data route `200`/`401` (pas 5xx) → `Stop terminé.`

- [ ] **Step 4: Commit**

```bash
cd /home/sylvain_ladoire/projects/developpeur/finance-tracker
git add README.md
git commit -m "docs+chore(local): documente le lancement local, supprime les launchers obsolètes

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Notes de migration (hors plan, pour mémoire)

Une fois l'app locale validée, le service Docker finance sur le NAS peut être arrêté
(`docker compose -f /volume2/docker/developpeur/finance-tracker-v2/docker-compose.yml down`).
**Ne pas supprimer les données NAS** avant d'avoir confirmé plusieurs jours d'usage local OK
(elles servent de backup). À décider avec l'utilisateur — hors scope de ce plan.
